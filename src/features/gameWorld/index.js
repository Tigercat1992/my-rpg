import React from 'react';

function GameWorld(props) {
	//const { player, largeView } = this.props;
	return(
		<div
			style={{
				position: 'relative',
			}}
		>
		</div>
	);
}

export default GameWorld;